  - platform: template
    name: Sliding Gate
    value_template: "{{ is_state('binary_sensor.sliding_gate_contact_sensor', 'off') }}"
    optimistic: true
    lock:
      service: switch.turn_on
      data:
        entity_id:  switch.sliding_gate_control
    unlock:
      service: switch.turn_on
      data:
        entity_id: switch.sliding_gate_control
